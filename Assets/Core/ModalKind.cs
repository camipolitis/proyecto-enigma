namespace Enigma.Core
{
    public enum ModalKind
    {
        Zoom = 0,
        Inventory = 1,
        Document = 2,
        Pause = 3,
        CodeEntry = 4
        // Password notebook / candado
    }
    // Orden numérico = prioridad... El stack real es LIFO
}
